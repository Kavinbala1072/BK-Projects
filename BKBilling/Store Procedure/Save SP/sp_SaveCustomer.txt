/*                                  
    Description       : Saving the Customer Creation (Insert/Update)
    Table             : Ledger_Table
    Generated By      : Kavin
    Generated ON      : 07/07/2026
    Remarks           : Handles Duplicate Code and Mobile validation per Company.
    Version           : 1
*/

CREATE PROCEDURE sp_SaveCustomer
    @Action NVARCHAR(10),
    @Ledger_Sno BIGINT = 0,
    @Company_No BIGINT,
    @User_No INT,
    @LedgerGroup_no INT = 1000000029,
    @ledger_name NVARCHAR(150),
    @ledger_code NVARCHAR(50),
    @Ledger_Phone NVARCHAR(20),
    @ledger_Active BIT,
    @Area_no INT,
    @ledger_Add1 NVARCHAR(200),
    @ledger_Add2 NVARCHAR(200),
    @ledger_Add3 NVARCHAR(200),
    @Ledger_Email NVARCHAR(100),
    @Ledger_ContactPerson NVARCHAR(100),
    @Balance_Type NVARCHAR(10),
    @Credit_Limit DECIMAL(18,2),
    @Credit_Days INT,
    @Ledger_GST NVARCHAR(15),
    @GST_DealerType NVARCHAR(20),
    @GST_StateCode NVARCHAR(5),
    @Ledger_open DECIMAL(18,2),
    @Ledger_remarks NVARCHAR(MAX)
AS
BEGIN
    SET NOCOUNT ON;

    -- 1. Validation: Check for Duplicate Ledger Code within the same company
    IF EXISTS (SELECT 1 FROM Ledger_Table WHERE Company_No = @Company_No AND ledger_code = @ledger_code AND Ledger_Sno <> @Ledger_Sno)
    BEGIN
        RAISERROR('The Ledger Code "%s" is already assigned to another account.', 16, 1, @ledger_code);
        RETURN;
    END

    -- 2. Validation: Check for Duplicate Mobile Number within the same company
    IF EXISTS (SELECT 1 FROM Ledger_Table WHERE Company_No = @Company_No AND Ledger_Phone = @Ledger_Phone AND Ledger_Sno <> @Ledger_Sno)
    BEGIN
        RAISERROR('The Mobile Number "%s" is already registered to another customer.', 16, 1, @Ledger_Phone);
        RETURN;
    END

    BEGIN TRANSACTION
    BEGIN TRY
        -- 3. Perform Action
        IF @Action = 'INSERT'
        BEGIN
            INSERT INTO Ledger_Table (
                Company_No, User_No, LedgerGroup_no, ledger_Active, ledger_code, ledger_name, 
                ledger_Add1, ledger_Add2, ledger_Add3, Area_no, Ledger_remarks, Ledger_Email, 
                Ledger_Phone, Ledger_ContactPerson, Balance_Type, Credit_Limit, Credit_Days, 
                Use_GST, Ledger_GST, GST_DealerType, GST_StateCode, Ledger_open, Created_Date, Modified_Date
            ) 
            VALUES (
                @Company_No, @User_No, @LedgerGroup_no, @ledger_Active, @ledger_code, @ledger_name, 
                @ledger_Add1, @ledger_Add2, @ledger_Add3, @Area_no, @Ledger_remarks, @Ledger_Email, 
                @Ledger_Phone, @Ledger_ContactPerson, @Balance_Type, @Credit_Limit, @Credit_Days, 
                1, @Ledger_GST, @GST_DealerType, @GST_StateCode, @Ledger_open, GETDATE(), GETDATE()
            );
        END
        ELSE IF @Action = 'UPDATE'
        BEGIN
            UPDATE Ledger_Table SET 
                User_No = @User_No,
                ledger_Active = @ledger_Active,
                ledger_code = @ledger_code,
                ledger_name = @ledger_name,
                ledger_Add1 = @ledger_Add1,
                ledger_Add2 = @ledger_Add2,
                ledger_Add3 = @ledger_Add3,
                Area_no = @Area_no,
                Ledger_remarks = @Ledger_remarks,
                Ledger_Email = @Ledger_Email,
                Ledger_Phone = @Ledger_Phone,
                Ledger_ContactPerson = @Ledger_ContactPerson,
                Balance_Type = @Balance_Type,
                Credit_Limit = @Credit_Limit,
                Credit_Days = @Credit_Days,
                Ledger_GST = @Ledger_GST,
                GST_DealerType = @GST_DealerType,
                GST_StateCode = @GST_StateCode,
                Modified_Date = GETDATE()
            WHERE Ledger_Sno = @Ledger_Sno AND Company_No = @Company_No;
        END

        COMMIT TRANSACTION
    END TRY
    BEGIN CATCH
        ROLLBACK TRANSACTION
        DECLARE @Err nvarchar(max) = ERROR_MESSAGE();
        RAISERROR(@Err, 16, 1);
    END CATCH
END